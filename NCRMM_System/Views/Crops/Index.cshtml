@model IEnumerable<NCRMM_System.Models.Crops_tbl>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainEmployeeLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CropsName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CropsCatagory_tbl.CropsCatagoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CropsName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CropsCatagory_tbl.CropsCatagoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CropsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CropsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CropsId })
        </td>
    </tr>
}

</table>
